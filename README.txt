DISCORD VERIFY BOT

Setup:
1. Install requirements:
   pip install -r requirements.txt

2. Edit bot.py:
   - Add your bot token
   - Add your role ID

3. Run:
   python bot.py

Commands:
!panel -> sends verify button (ADMIN ONLY)
